name: Next.js bereitstellen und Datenbank migrieren

on:
  workflow_dispatch:
    inputs:
      version:
        description: "1.00"
        required: true
        type: string

permissions:
  contents: read
  packages: write

concurrency:
  group: project-nextjs-production
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Version prüfen
        env:
          VERSION: ${{ inputs.version }}
        run: |
          set -Eeuo pipefail

          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Ungültige Version: $VERSION"
            exit 1
          fi

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Abhängigkeiten installieren
        run: npm ci

      - name: Prisma Client generieren
        run: npx prisma generate

      - name: ESLint
        run: npm run lint

      - name: TypeScript prüfen
        run: npm run typecheck


      - name: Buildx einrichten
        uses: docker/setup-buildx-action@v3

      - name: Bei GHCR anmelden
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Migrations-Image bauen und pushen
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./services/migration/Dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ghcr.io/alexschneider-dev/project-migration:${{ inputs.version }}
            ghcr.io/alexschneider-dev/project-migration:${{ github.sha }}
          cache-from: type=gha,scope=project-migration
          cache-to: type=gha,mode=max,scope=project-migration

      - name: Next.js image bauen und pushen
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./services/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/alexschneider-dev/project:${{ inputs.version }}
            ghcr.io/alexschneider-dev/project:${{ github.sha }}
          cache-from: type=gha,scope=project-nextjs
          cache-to: type=gha,mode=max,scope=project-nextjs

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   timeout-minutes: 15
  #   environment: production

  #   steps:
  #     - name: SSH konfigurieren
  #       env:
  #         PROD_SSH_KEY: ${{ secrets.PROD_SSH_KEY }}
  #         PROD_KNOWN_HOSTS: ${{ secrets.PROD_KNOWN_HOSTS }}
  #       run: |
  #         set -Eeuo pipefail

  #         install -m 700 -d ~/.ssh

  #         printf '%s\n' "$PROD_SSH_KEY" > ~/.ssh/id_ed25519
  #         chmod 600 ~/.ssh/id_ed25519

  #         printf '%s\n' "$PROD_KNOWN_HOSTS" > ~/.ssh/known_hosts
  #         chmod 600 ~/.ssh/known_hosts

  #     - name: Datenbank-Backup erstellen
  #       env:
  #         PROD_HOST: ${{ secrets.PROD_HOST }}
  #         PROD_USER: ${{ secrets.PROD_USER }}
  #       run: |
  #         set -Eeuo pipefail

  #         ssh \
  #           -i ~/.ssh/id_ed25519 \
  #           -o BatchMode=yes \
  #           -o StrictHostKeyChecking=yes \
  #           "${PROD_USER}@${PROD_HOST}" \
  #           "sudo -n /usr/local/lib/project-deploy/backup-before-main-deploy.sh"

  #     - name: Datenbankmigration ausführen
  #       env:
  #         PROD_HOST: ${{ secrets.PROD_HOST }}
  #         PROD_USER: ${{ secrets.PROD_USER }}
  #         VERSION: ${{ inputs.version }}
  #       run: |
  #         set -Eeuo pipefail

  #         ssh \
  #           -i ~/.ssh/id_ed25519 \
  #           -o BatchMode=yes \
  #           -o StrictHostKeyChecking=yes \
  #           "${PROD_USER}@${PROD_HOST}" \
  #           "sudo -n /usr/local/lib/project-deploy/migrate.sh '${VERSION}'"

  #     - name: Auf Produktion deployen
  #       env:
  #         PROD_HOST: ${{ secrets.PROD_HOST }}
  #         PROD_USER: ${{ secrets.PROD_USER }}
  #         VERSION: ${{ inputs.version }}
  #       run: |
  #         set -Eeuo pipefail

  #         ssh \
  #           -i ~/.ssh/id_ed25519 \
  #           -o BatchMode=yes \
  #           -o StrictHostKeyChecking=yes \
  #           "${PROD_USER}@${PROD_HOST}" \
  #           "sudo -n /usr/local/lib/project-deploy/deploy.sh nextjs '${VERSION}'"

  #     - name: Website prüfen
  #       run: |
  #         curl \
  #           --silent \
  #           --show-error \
  #           --fail \
  #           --retry 20 \
  #           --retry-delay 3 \
  #           --retry-all-errors \
  #           --connect-timeout 5 \
  #           --max-time 15 \
  #           https://alex-schneider-dev.de/ \
  #           >/dev/null

  #         echo "✅ Produktion ist erreichbar"